# Control plane API base URL (proxied through Nginx in production)
VITE_API_URL=http://localhost:3000

# Use mock sessions in the dashboard for development and testing
VITE_DASHBOARD_USE_MOCK_SESSIONS=false

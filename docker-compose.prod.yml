services:
  nginx:
    image: nginx:1.28-alpine
    container_name: syncode-nginx
    restart: unless-stopped
    ports:
      - "${NGINX_PORT:-4443}:80"
    volumes:
      - ./infra/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      web:
        condition: service_started
      control-plane:
        condition: service_healthy
      collab-plane:
        condition: service_healthy
    networks:
      - frontend
      - proxy

  web:
    build:
      context: .
      dockerfile: infra/docker/Dockerfile.web
    container_name: syncode-web
    restart: unless-stopped
    networks:
      - frontend

  control-plane:
    build:
      context: .
      dockerfile: infra/docker/Dockerfile.control-plane
    container_name: syncode-control-plane
    restart: unless-stopped
    env_file: .env
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - proxy
      - backend

  collab-plane:
    build:
      context: .
      dockerfile: infra/docker/Dockerfile.collab-plane
    container_name: syncode-collab-plane
    restart: unless-stopped
    env_file: .env
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - proxy
      - backend

  execution-plane:
    build:
      context: .
      dockerfile: infra/docker/Dockerfile.execution-plane
    container_name: syncode-execution-plane
    restart: unless-stopped
    env_file: .env
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - backend

  ai-plane:
    build:
      context: .
      dockerfile: infra/docker/Dockerfile.ai-plane
    container_name: syncode-ai-plane
    restart: unless-stopped
    env_file: .env
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - backend

  postgres:
    image: postgres:17-alpine
    container_name: syncode-postgres-prod
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:?POSTGRES_USER is required}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}
      POSTGRES_DB: ${POSTGRES_DB:-syncode}
    volumes:
      - pg_data:/var/lib/postgresql/data
    networks:
      - backend
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: syncode-redis-prod
    restart: unless-stopped
    environment:
      REDIS_PASSWORD: ${REDIS_PASSWORD:?REDIS_PASSWORD is required}
    command: >
      redis-server
      --appendonly yes
      --requirepass ${REDIS_PASSWORD:?REDIS_PASSWORD is required}
      --maxmemory 256mb
      --maxmemory-policy allkeys-lru
    volumes:
      - redis_data:/data
    networks:
      - backend
    healthcheck:
      test: ["CMD-SHELL", 'redis-cli -a "$$REDIS_PASSWORD" ping']
      interval: 5s
      timeout: 5s
      retries: 5

  livekit:
    image: livekit/livekit-server:v1.9.11
    container_name: syncode-livekit
    restart: unless-stopped
    environment:
      LIVEKIT_KEYS: "${LIVEKIT_API_KEY:?LIVEKIT_API_KEY is required}: ${LIVEKIT_API_SECRET:?LIVEKIT_API_SECRET is required}"
    networks:
      - proxy
      - backend

  seaweedfs:
    image: chrislusf/seaweedfs:4.13
    container_name: syncode-seaweedfs
    restart: unless-stopped
    command: server -s3
    volumes:
      - seaweedfs_data:/data
    networks:
      - backend
    healthcheck:
      test:
        [
          "CMD",
          "wget",
          "--spider",
          "-q",
          "http://localhost:9333/cluster/status",
        ]
      interval: 10s
      timeout: 5s
      start_period: 10s
      retries: 3

networks:
  frontend:
    driver: bridge
  proxy:
    driver: bridge
  backend:
    driver: bridge

volumes:
  pg_data:
  redis_data:
  seaweedfs_data:
